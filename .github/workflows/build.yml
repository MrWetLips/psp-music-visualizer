- name: Build
        run: |
          export PSPDEV=/usr/local/pspdev
          export PATH=$PATH:$PSPDEV/bin
          make
          echo "=== PARAM.SFO ==="
          psp-sfo PARAM.SFO || true
          echo "=== File sizes ==="
          ls -la EBOOT.PBP retroviz.elf
