- name: Build and package
        run: |
          export PSPDEV=/usr/local/pspdev
          export PATH=$PATH:$PSPDEV/bin
          make
          echo "=== ELF info ==="
          psp-readelf -h retroviz.elf
          echo "=== Imports ==="
          psp-readelf -d retroviz.elf | grep NEEDED
          echo "=== Section list ==="
          psp-readelf -S retroviz.elf | grep -E "lib|prx|module"
          ls -la EBOOT.PBP
